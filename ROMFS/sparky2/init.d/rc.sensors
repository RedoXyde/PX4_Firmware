#!nsh
#
# Standard startup script for Sparky2 onboard sensor drivers.
#

if ms5611 start
then
fi

if adc start
then
fi

# MPU9250
if mpu9250 start
then
fi

if meas_airspeed start
then
else
	if ets_airspeed start
	then
	else
		if ets_airspeed start -b 1
		then
		fi
	fi
fi

#
# Start sensors
#

# Wait 20 ms for sensors (because we need to wait for the HRT and work queue callbacks to fire)
usleep 20000
if sensors start
then
fi
